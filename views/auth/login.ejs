<% const safePrefillEmail = typeof prefillEmail !== 'undefined' ? prefillEmail : ''; %>
<% const body = include('../partials/loginContent', { prefillEmail: safePrefillEmail }); %>
<%- include('../layout', { pageClass: 'page-auth', body, metaTitle: 'Connexion – OLYMP' }) %>
